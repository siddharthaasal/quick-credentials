

export default function CopyComponent({ name, val }) {
    return (
        <>
            <div className="items-center cursor-pointer">
                <p>{name}</p>
            </div>

        </>
    )
}